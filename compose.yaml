services:
  api:
    build: .
    container_name: trongledex-api
    command: fastapi dev app/main.py
    volumes:
      - db:/code/db
    ports:
      - 8000:8000
    develop:
      watch:
        - action: rebuild
          path: .
volumes:
  db:
